import { NullCierrePipe } from './null-cierre.pipe';

describe('NullCierrePipe', () => {
  it('create an instance', () => {
    const pipe = new NullCierrePipe();
    expect(pipe).toBeTruthy();
  });
});
